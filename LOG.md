## [2026-06-23] lint   | Added five inline-SVG figures to concept pages where a picture beats prose, one design agent per page against the DIAGRAMS.md bar, then gated each through the slop-verifier (all PASS, range 87-88). The five: `permission-boundary` (boundary-before / gate-during / trail-after on one time axis), `context-window` (nested containment: connected sources contain the retrievable set contain what's actually in the window this turn, with selection as the inward arrow), `agent-workspaces` (same model both sides; stateless transcript vs coupling to durable files/shell/memory), `rollback` (reversibility spectrum where the wide partial middle, not the clean ends, is where most real writes land), and `mcp` (the protocol carries the call on the wire; scope/permissions/approval/audit sit in a layer above it). All use the house sketch-board idiom (shared `.sketch-*` classes, two-accent palette, theme-flipping `var(--light)` text on accent fills so contrast holds in both modes). Polished two captions the verifier flagged: dropped a thrice-stated thesis in `context-window`'s title and changed `mcp`'s "governance gap" to the body-accurate "governance layer". Left the 12 other figureless concepts as prose (node/connector pages and the governance cluster already covered by the boundary triptych). Quartz build clean; all figures render as real SVG, none trapped in code blocks.

## [2026-06-22] lint   | Quality pass over all 22 `content/concepts/` pages, one editing agent per page against the anti-slop bar, then gated every page through the slop-verifier (publish floor 80). All 22 clear it (range 84-88, median 87). De-slopped three that initially fell short: `orchestrating-scoped-agents` (broke a rule-of-three habit, 78 to 87), `memory-promotion-pipeline` (cut a 3x-repeated sentence and adjacent triads, 78 to 86; web-verified the two external claims, Anthropic Dreams and the Azure SRE ~30-min idle consolidation, both accurate and kept), and `the-agent-as-semantic-ui` (rebuilt the inline-SVG figure so it shows User intent then GUI/Semantic UI then one governed execution path, matching the argument; unbolded a key-takeaway sentence). No new figures added anywhere else: prose carried the rest. Fixed a YAML build break in `write-access` (unquoted colon in the description). Quartz build clean; all five inline-SVG figures render as real SVG, none as code blocks.

## [2026-06-13] lint   | Connected the "dreaming"/memory-consolidation idea into the memory cluster instead of leaving it stranded in `approval-gate`. Made `memory-promotion-pipeline` the canonical home with a new "What actually does the climbing" section: the consolidation pass (Anthropic Dreams, Azure SRE) is the engine that automates the early unsupervised promotion steps (dedupe, generalize, retire stale) and stops exactly at the human gate, since Dreams emits a reviewable candidate and never mutates the source store. Cross-linked `agent-trace` (traces are the pass's input), `approval-gate` (corrections are its feedstock), and `federated-memory` (retiring stale lessons is what the pass automates) to that home.

## [2026-06-13] ingest | Filled the 20 draft placeholders left by the IA refactor, working from a critical interview with Floris. Wrote nine concept nodes as finished prose in his voice (`write-access`, `system-of-record`, `permission-boundary`, `approval-gate`, `audit-trail`, `mcp`, `agent-trace`, `rollback`, `context-window`) and five short defer-to-essay nodes (`scoped-agent`, `process-orchestrator`, `workspace-state`, `tool-calling`, `tool-schema`) that point at the existing rich essays. Wrote the field note `why-write-access-is-hard-in-regulated-environments` (the blocker isn't the regulator: orgs don't know how yet, the APIs were built for deterministic callers, and the deterministic workflow/API layer stays; the agent's place is retrieval and intent, not the execution seat). De-drafted the five map pages and refreshed homepage labels. Removed `draft: true` across content; no em dashes. Cited the Azure SRE agent's cross-session memory and post-thread consolidation pass plus Anthropic's "Dreams" feature in `approval-gate` (the "dreaming" memory-consolidation idea is a real named feature, not just a metaphor).

## [2026-06-12] ingest | Refactored the wiki information architecture around content types. Added draft map pages (`enterprise-ai-map`, `agent-architecture-map`, `interfaces-map`, `memory-map`, `governance-map`), a draft field-note placeholder (`why-write-access-is-hard-in-regulated-environments`), and reusable map/concept templates under `wiki/templates/`. Seeded concrete draft concept nodes (`mcp`, `tool-calling`, `tool-schema`, `context-window`, `permission-boundary`, `approval-gate`, `audit-trail`, `agent-trace`, `scoped-agent`, `process-orchestrator`, `write-access`, `rollback`, `system-of-record`, `workspace-state`). Added `status` and `type` frontmatter to existing core thought/concept pages and updated homepage navigation to expose map, concept, essay, claim, and field-note lanes.

## [2026-06-11] ingest | Wove Mukul Singh's "MCP Is Not Enough: Why Code DSLs Will Replace JSON Tool Calling" into the tool-calling concept cluster. Added an MCP-grounded paragraph to `concepts/json-as-transport-not-cognition` (MCP is the JSON-RPC layer; multi-step chains are death by round trips; code execution batches them while JSON stays underneath as transport) and a linked sentence to `concepts/cli-as-compressed-action-language` (code is where tool-calling protocols are drifting). Cited the article plus Anthropic's code-execution-with-MCP and Cloudflare's code-mode. TODO(floris): file the raw archived copy under `raw/` and pull in the article's specific benchmark numbers once verified.

## [2026-06-11] lint | Moved `human-tools-not-machine-protocols` from `concepts/` to `thoughts/`. The page threads CLI, JSON, and browser-use concepts into a broader argument; that's a thought, not a concept.

## [2026-06-11] init | Scaffolded the garden (Quartz v4, homepage, About, How-this-works, first thought, AGENTS.md schema).

## [2026-06-11] design | Reskinned the site as quiet frontier editorial (warm paper palette, Literata/Source Sans 3, restrained accents); rewrote homepage as masthead + start-here index.

## [2026-06-11] design | Pushed a touch further into Fluent: gave the UI chrome a Segoe UI Variable font (left explorer, breadcrumbs, content meta, right-rail headings, TOC, backlinks) while content keeps the editorial serif, turned the active explorer item into a Fluent NavView style rounded blue selection, made the section labels uppercase Segoe, and unified all chrome hover/focus/in-view states on the Microsoft blue (plus a soft Fluent focus halo). Reads as a Fluent app shell around an editorial document.

## [2026-06-11] design | Added a muted Microsoft blue (--ms) routed only through interaction states (link hover, focus rings, text selection, masthead nav hover, reading-progress bar, diagram expand hover). Olive stays the resting editorial accent; the blue reads as a quiet Fluent cue without going corporate.

## [2026-06-11] design | Made Mermaid diagrams clean: themed nodes/edges to the warm palette, removed the code-box chrome so diagrams rest on the paper, added a quiet loading cue (no raw-source flash), and replaced the frosted-glass fullscreen modal with a warm ink scrim. Converted the publish-flow on How-this-works into a real diagram.

## [2026-06-11] schema | Made the AI system consistent with the LLM-wiki pattern: added a Skills layer (`.github/skills/wiki-ingest`, `wiki-query`, `wiki-lint`) implementing the Ingest/Query/Lint operations; rewrote `wiki-librarian` as a thin persona that routes to them; created the referenced `raw/` and `wiki/` layers; and documented the instructions-vs-agents-vs-skills split in AGENTS.md, copilot-instructions, AGENTIC-PIPELINE, and README.

## [2026-06-11] ingest | Seeded the first real cluster: hub Thought essay `thoughts/from-chatbots-to-system-operators` plus 7 atomic Concept pages under `content/concepts/` (json-as-transport-not-cognition, cli-as-compressed-action-language, agent-workspaces, scoped-system-specialist-agents, the-agent-as-semantic-ui, federated-memory-for-enterprise-agents, memory-promotion-pipeline). Introduced the `/concepts` content type and a shared `agentic-ai` tag as the auto-index; deferred `/maps` and source notes by design.

## [2026-06-11] ingest | Rewrote `content/about.md` from placeholder template into real prose, grounded in Floris's positioning source (Microsoft / regulated financial services, CS + data science background, RL-for-agentic-KGQA thesis presented at ISWC 2025 Nara). Positioned the blog as a notebook on the gap between AI demos and production systems; linked the system-operators essay; added LinkedIn. slop-verifier: PASS (85).
## [2026-06-11] ingest | Expanded the system-operators cluster from a compressed thought-note into a full index essay (per editorial critique in #14): added reader-tension opening, model↔environment co-evolution nuance, a JSON-vs-CLI example, counterarguments (JSON is useful, CLI is dangerous, GUI stays, scope fragments), a layered architecture, the orchestration pattern (broad intent, narrow execution), and an expanded memory section. Deepened all 7 concept pages with examples + counterarguments, added a new `orchestrating-scoped-agents` concept, and added three earned diagrams (evolution + architecture in the essay, memory-promotion pipeline on its page).

## [2026-06-11] ingest | Landed a new concept page `concepts/human-tools-not-machine-protocols` (the observation that agents adopt the tools humans built, like the shell, SQL, and the browser, rather than the machine-to-machine protocols they were designed to avoid). Wired it into the system-operators essay and the index. Removed every em-dash across `content/` per editorial preference, rewriting each as commas, colons, semicolons, or parentheses.

## [2026-06-11] build | Shipped the AG-UI + Foundry hosted-agent showcase. `agent/` is a Microsoft Agent Framework agent (FoundryChatClient over a gpt-4o-mini Foundry deployment) grounded by in-process lexical retrieval over baked `content/`, exposed over AG-UI via add_agent_framework_fastapi_endpoint, deployed to Azure Container Apps (scale-to-zero, managed-identity auth, no secrets) with azd/Bicep in `infra/`. The garden-side widget is one Quartz island (`GardenAgent.tsx` + inline script on @ag-ui/client) that streams cited answers and renders an editable issue-draft card handing off to GitHub's prefilled new-issue URL. Verified end to end against the live endpoint: health, CORS preflight for both origins, AG-UI SSE run with a grounded citation and a draft_github_issue tool call. Wired `agent-deploy.yml` to azd over GitHub OIDC (Entra app + federated credential + repo variables in the demo tenant). Blueprint updated to the as-built design.
