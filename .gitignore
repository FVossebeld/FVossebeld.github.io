# Quartz build + dependencies
public/
node_modules/
.quartz-cache/

# OS / editor noise
.DS_Store
*.log
.vscode/

# azd local environment state (no secrets in repo)
.azure/

# compiled Bicep
infra/*.json
!infra/main.parameters.json
